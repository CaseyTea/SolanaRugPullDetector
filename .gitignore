bin/
obj/
*.user
*.suo
.vs/
.idea/
*.DS_Store

# Training data (34MB+ CSV, users generate from SolRPDS repo)
*.csv

# Trained model output (users retrain locally)
rugpull_model.zip

# Dataset repo (cloned separately)
SolRPDS/

# Environment files — never commit secrets
# .env.example IS committed as a template (see ! rule below)
.env
.env.*
!.env.example
